# XXX - just placeholders for now
def require_app_read(func):
    def wrapped(*args, **kwargs):
        return func(*args, **kwargs)

    return wrapped


def require_app_write(func):
    def wrapped(*args, **kwargs):
        return func(*args, **kwargs)

    return wrapped
